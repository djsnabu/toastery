---
import UnderlineHeading from './UnderlineHeading.astro';
import PostCard from './PostCard.astro';
import { journal } from '../content/copy.en';
import { getPublishedPosts } from '../utils/blog';

const posts = (await getPublishedPosts()).slice(0, 3);
---

{
  posts.length > 0 && (
    <section class="latest section">
      <div class="container">
        <p class="label">{journal.label}</p>
        <UnderlineHeading as="h2">{journal.title}</UnderlineHeading>
        <p class="latest__intro">{journal.intro}</p>
        <div class="latest__grid">
          {posts.map((post, i) => (
            <PostCard post={post} featured={i === 0} />
          ))}
        </div>
        <p class="latest__more">
          <a class="link-arrow" href="/blog">
            {journal.readAll}
          </a>
        </p>
      </div>
    </section>
  )
}

<style>
  .latest .heading-serif {
    font-size: clamp(2rem, 4vw, 2.75rem);
    margin-top: 0.25rem;
  }

  .latest__intro {
    max-width: 40ch;
    color: var(--toastery-grey);
    margin: var(--space-md) 0 var(--space-lg);
  }

  .latest__grid {
    display: grid;
    gap: var(--space-lg);
  }

  @media (min-width: 768px) {
    .latest__grid {
      grid-template-columns: 1.2fr 1fr 1fr;
    }

    .latest__grid :global(.post-card:first-child) {
      grid-row: span 2;
    }

    .latest__grid :global(.post-card:first-child .post-card__image) {
      aspect-ratio: 3 / 4;
      min-height: 100%;
      object-fit: cover;
    }
  }

  .latest__more {
    margin-top: var(--space-lg);
  }
</style>
